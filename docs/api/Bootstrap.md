# Bootstrap
**Namespace:** `Indieinabox`

Class Bootstrap

Encapsulates application initialization and dependency setup:
- Loads procedural function files and helpers.
- Initializes configuration and default values.
- Prepares dynamic global configuration arrays (`$kindspath`, `$urltranslations`).
- Sets up error handling and environment constants.
