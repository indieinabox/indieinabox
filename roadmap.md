# Roadmap

## Localization & Translation
- [ ] Replace `pseudoTranslate()` in `SiteBuilder.php` with a proper translation mechanism (e.g. LLM integration or gettext/machine translation API). Currently it just prefixes the title/content with `[LANG] `.
